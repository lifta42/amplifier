; main
`put `+ `- `* `/ `< `= `then `id `Y `cons `car `cdr
  `list
    put, car, cdr list
  ., cons 42, cons 65, cons 44, nil
.............

; put + - * / < =
`x x put.
`a `b b, a +..
`a `b b, a -..
`a `b b, a *..
`a `b b, a /..
`a `b b, a <..
`a `b b, a =..

; then
`x `y y..

; id
`x x.

; Y
`f
  `x f, x x.
  `x f, x x.
.

; cons car cdr
`h `t `f f h t...
`l l `h `t h.. .
`l l `h `t t.. .

.

; main
`put `+ `- `* `/ `< `= `then `id
  `Y
    Y `self `n `m
      `keep-going
        n < m ? keep-going 0
      ., ; keep-going
      put n then m, n + 1, self
    ... 33 127 then put 10
  .
  ; Y
  `f
    `x f, x x.
    `x f, x x.
  .
.........

; put + - * / < =
`x x put.
`a `b b, a +..
`a `b b, a -..
`a `b b, a *..
`a `b b, a /..
`a `b b, a <..
`a `b b, a =..

; then
`x `y y..

; id
`x x.

; true false


.

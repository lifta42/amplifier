; main
`put `+ `- `* `/ `< `= `then `id
  put 42 then put 10
.........

; put + - * / < =
`x x put.
`a `b b, a +..
`a `b b, a -..
`a `b b, a *..
`a `b b, a /..
`a `b b, a <..
`a `b b, a =..

; then
`x `y y..

; id
`x x.

.

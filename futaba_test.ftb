; main
`put `+ `- `* `/ `< `= `then `id
  `iter'
    iter' iter' 65 then put 10
  .
  ; iter'
  `self `n
    `keep
      n < 97 ? keep 0
    ., ; keep
    put n then n + 1, self self
  ..
.........

; put + - * / < =
`x x put.
`a `b b, a +..
`a `b b, a -..
`a `b b, a *..
`a `b b, a /..
`a `b b, a <..
`a `b b, a =..

; then
`x `y y..

; id
`x x.

; true false


.
